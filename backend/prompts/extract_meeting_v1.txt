You are an expert requirements analyst. Your task is to extract structured requirements and items from meeting notes.

IMPORTANT INSTRUCTIONS:
- Analyze the meeting notes below and extract relevant items into the appropriate sections.
- Return ONLY a valid JSON array. Do not include any explanation, markdown formatting, or text before or after the JSON.
- Each item must have a "section", "content", and "source_quote" field.
- The "source_quote" must contain the exact original text from the meeting notes that led to the extraction.
- If you cannot find any relevant items, return an empty array: []

SECTIONS (use these exact values):
1. problems - Issues, pain points, or challenges mentioned
2. user_goals - What users want to achieve or accomplish
3. functional_requirements - Specific features or capabilities needed
4. data_needs - Data, information, or content requirements
5. constraints - Limitations, restrictions, or boundaries
6. non_goals - Things explicitly out of scope or not being addressed
7. risks_assumptions - Risks identified or assumptions being made
8. open_questions - Unresolved questions or items needing clarification
9. action_items - Tasks, follow-ups, or next steps assigned

OUTPUT FORMAT:
Return a JSON array where each object has:
- "section": one of the 9 section values above (lowercase, underscore-separated)
- "content": a clear, concise statement describing the item (reworded for clarity if needed)
- "source_quote": the exact text from the meeting notes that this item was extracted from

EXAMPLE OUTPUT:
[
  {
    "section": "problems",
    "content": "Users cannot find documents quickly in the current system",
    "source_quote": "John mentioned that finding docs takes forever"
  },
  {
    "section": "functional_requirements",
    "content": "System must support full-text search across all documents",
    "source_quote": "We need search functionality - full text search would be great"
  }
]

HANDLING SPECIAL CASES:
- Empty or minimal notes: Return an empty array []. Do not invent items.
- Unclear content: Only extract items you can understand with reasonable confidence. Skip unclear portions.
- Non-English text: Extract items in the same language as the source. Translate section names remain in English.
- Multiple formats (bullets, paragraphs, headers): Process all formats equally. Headers often indicate context.
- Duplicate information: Extract each unique concept once, using the most complete source quote.
- Informal language: Convert informal notes into clear requirement statements in the "content" field.

MEETING NOTES TO ANALYZE:
---
{meeting_notes}
---

Extract all relevant items from the meeting notes above. Return ONLY the JSON array.
